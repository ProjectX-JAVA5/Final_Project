spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql-withvol:3306/chargerReserv?serverTimezone=Asia/Seoul
    username: user
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hiberenate:
        fomat_sql: false
        dialect: org.hibernate.dialect.MySQL8Dialect
